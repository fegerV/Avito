# Анализ фронтенд кодовой базы: Avito Clone Frontend

## 📁 Структура проекта
```
frontend
├── .env.local
├── Dockerfile
├── next.config.js
├── package.json
├── postcss.config.js
├── tailwind.config.js
├── tsconfig.json
├── src
│   ├── app
│   │   ├── globals.css
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   ├── login
│   │   │   └── page.tsx
│   │   ├── register
│   │   │   └── page.tsx
│   │   └── listings
│   │       └── page.tsx
│   ├── lib
│   │   └── api.ts
│   ├── store
│   │   └── auth.ts
│   └── types
│       └── index.ts
```

**Назначение директорий (уровень до 3):**
- `src/app` — реализация Next.js App Router: глобальный layout, глобальные стили и страницы (`/`, `/login`, `/register`, `/listings`). Компоненты помечены `use client`, что подчёркивает ориентацию на клиентский рендеринг и доступ к `localStorage`.
- `src/lib` — инфраструктурные модули. Сейчас содержит единую точку для HTTP-клиента (`api.ts`), куда можно добавлять перехватчики, базовые URL и заголовки.
- `src/store` — глобальные состояния на Zustand (пока только `useAuth`).
- `src/types` — общие TypeScript интерфейсы для сущностей (Listing, Category, User и т.д.).
- Корневые конфигурации (`next.config.js`, `tailwind.config.js`, `tsconfig.json`, `.eslintrc.json`, `.prettierrc`) определяют единые правила сборки, стилей и линтинга; `Dockerfile` описывает контейнеризацию Node 20 + Next standalone.

**Принцип организации кода:** гибридная схема «router-based + lightweight layers». Маршруты группируются по сегментам внутри `app`, а переиспользуемые слои вынесены в `lib`, `store`, `types`. Такой подход хорошо масштабируется для MVP, но по мере роста проекта потребуется либо feature-based структура, либо модульные папки (например, `modules/listings`) для инкапсуляции UI, стора и API конкретного домена.

## 🛠 Технологический стек
| Категория | Технология / версия | Назначение |
| --- | --- | --- |
| Фреймворк | Next.js `^14.0.3` (App Router, React 18.2, SWC) | Серверный и клиентский рендеринг, маршрутизация, сборка.
| Язык | TypeScript `^5.2.2` (strict mode, `paths: @/*`) | Типобезопасность компонентов, стора и API.
| Стили | Tailwind CSS `^3.4.1` + PostCSS/Autoprefixer | Утилитарная стилизация, тема с кастомными цветами `primary/secondary`.
| Состояние | Zustand `^4.4.2` | Лёгкое глобальное состояние для аутентификации, управление `isLoading`/`error`.
| Сетевой слой | Axios `^1.6.5` | HTTP-клиент с перехватчиком Authorization и динамическим `NEXT_PUBLIC_API_URL`.
| UI-утилиты | `clsx`, `react-icons`, `react-hot-toast`, `date-fns` | Подготовлены для композиции классов, иконок, тостов и форматирования дат (на текущий момент используются частично).
| Сборка/CLI | `next dev/build/start`, `npm` | Стандартные сценарии разработки и production-сборки.
| Качество кода | ESLint `^8.54.0` (`next/core-web-vitals`), Prettier `^3.1.0`, `tsc --noEmit` | Поддержание стиля и типов, есть скрипты `lint`, `lint:fix`, `format`, `type-check`.
| CSS-постпроцессинг | PostCSS `^8.4.32`, `tailwind.config.js` | Генерация классов, поддержка современных свойств.
| Контейнеризация | Dockerfile (Node 20-alpine, multi-stage) | Детеминированная prod-сборка `next build` + запуск через `server.js`.

## 🏗 Архитектура
Архитектура соответствует небольшому Next.js MVP: App Router управляет страницами, состояние сведено к одной Zustand-сторе, а работу с API инкапсулирует axios-инстанс. Основные принципы:

### Компонентная архитектура и роутинг
- Каждая страница реализована как клиентский компонент (`'use client'`), что даёт доступ к хранилищам браузера и упрощает взаимодействие с формами.
- Страницы находятся в изолированных сегментах (`/login`, `/register`, `/listings`) и используют общие UI-паттерны (карточки, формы, фильтры) на Tailwind.
- Глобальный `layout.tsx` подключает `globals.css` и задаёт общую мета-информацию.

### Разделение логики и состояние
- Общая авторизационная логика вынесена в `useAuth` (Zustand). Стор отслеживает `user`, `isAuthenticated`, `isLoading`, `error` и предоставляет методы `register`, `login`, `logout`, `checkAuth`.
- Локальное состояние компонентов (`useState`) обрабатывает форму ввода и фильтры (`formData`, `search`, `minPrice` и т.д.), что делает компоненты самодостаточными, но приводит к дублированию (например, похожие поля в логине и регистрации).

### API-слой и работа с данными
- `src/lib/api.ts` создаёт axios-инстанс с базовым URL из `NEXT_PUBLIC_API_URL` и добавляет Bearer-токен в заголовок каждого запроса.
- Стор и страницы вызывают REST-эндпоинты (например, `/auth/register`, `/listings/search`). Типы (`Listing`, `User`) помогают документировать структуру ответа, но не используются для runtime-валидации.
- Поисковые фильтры собираются через `URLSearchParams`, после чего выполняется один HTTP-вызов с готовой строкой запроса.

**Пример шаблона fetching + фильтрация (<15 строк):**
```ts
const fetchListings = async () => {
  setLoading(true);
  try {
    const params = new URLSearchParams();
    if (search) params.append('query', search);
    if (region) params.append('region', region);
    if (minPrice) params.append('minPrice', minPrice);
    if (maxPrice) params.append('maxPrice', maxPrice);
    const response = await api.get(`/listings/search?${params.toString()}`);
    setListings(response.data.listings);
  } finally {
    setLoading(false);
  }
};
```

### Обработка ошибок и загрузок
- В `useAuth` ошибки ловятся в catch-блоках и прокидываются в стейт, который затем отображается в компонентах (`RegisterPage`, `LoginPage`).
- `ListingsPage` управляет `loading` и fallback UI («Loading listings…», «No listings found»), но ошибки выводятся только в консоль.
- Глобальных error/loading boundaries нет; один неудачный запрос не блокирует весь UI, но пользователю не всегда видно, что пошло не так.

### Паттерны и best practices
- Использование Zustand с императивным API делает логику авторизации легко расширяемой (можно добавить refresh-token, профили и т.п.).
- Tailwind-классы позволяют быстро собирать адаптивные сетки (`grid-cols-1 md:grid-cols-2 lg:grid-cols-3`).
- Типы сущностей вынесены в `src/types`, что упрощает повторное использование между компонентами и сторами.
- Axios-интерцептор централизует работу с токеном и избавляет от дублирования заголовков.

### Зоны роста и риски
- Доступ к `localStorage` в `api.ts` выполняется на уровне модуля (при импорте), что несовместимо с серверным рендерингом и может вызвать ReferenceError при рендере на сервере. Следует либо ограничить вызовы `api` клиентскими компонентами, либо добавить guard `if (typeof window !== 'undefined')`.
- Отсутствие дебаунса приводит к множественным HTTP-вызовам во время ввода фильтров.
- Нет механизма кеширования (SWR/React Query), поэтому каждый переход на страницу заново тянет данные.
- Роутинг пока не покрывает динамические страницы (`/listings/[id]` упоминается в ссылке, но файла нет), что приведёт к 404.

## 🎨 UI/UX и стилизация
- **Подход:** Tailwind CSS формирует весь UI; кастомные цвета `primary/secondary` определены в `tailwind.config.js`. Дополнительно подключён лёгкий глобальный reset в `globals.css`.
- **Компоненты:** Формы и карточки оформлены стандартными Tailwind-классами (`rounded-lg`, `shadow-xl`, `focus:border-blue-500`). Повторяющиеся куски UI (nav, карточка объявления) пока не вынесены в компоненты.
- **Адаптивность:** Используются responsive-классы (`md:grid-cols-4`, `lg:grid-cols-3`, `md:grid-cols-2`), поэтому основные разделы ведут себя корректно на планшетах/desktop. На мобильных возможны проблемы с крупными паддингами и отсутствием стэк-ориентированных навигационных элементов.
- **Дизайн-система/UI-kit:** отдельной дизайн-системы нет; UI строится на Tailwind-утилитах. Дополнительные библиотеки (`react-icons`, `react-hot-toast`) пока не задействованы.
- **Темизация:** отсутствует; можно добавить поддержку CSS-переменных или Tailwind `dark`-variants.
- **Доступность:** базовая семантика соблюдена (используются `<nav>`, `<main>`, `<form>`), но нет aria-атрибутов для сообщений об ошибках, кнопок-иконок и индикаторов загрузки.

## ✅ Качество кода
- **Линтеры и форматирование:** ESLint расширяет `next/core-web-vitals`, отключая лишь `@next/next/no-html-link-for-pages` (не актуально для App Router). Prettier выравнивает стиль (`singleQuote`, `trailingComma: all`). Есть отдельные скрипты `lint`, `lint:fix`, `format` и `type-check`.
- **Типизация:** `tsconfig` включает строгий режим. Типы сущностей хорошо задокументированы, но компоненты часто используют не типизированные локальные состояния (`useState('')`). Нет типов для ответов API (используются `any` в catch-блоках) и для форм (`React.FormEvent`).
- **Тестирование:** unit/integration/e2e тесты отсутствуют, конфигурации Jest/Playwright не найдено. Ключевой функционал (логин, поиск) не покрыт автоматическими проверками.
- **Документация:** внутри кода комментарии минимальны; rely на читаемость и именование. Общая документация проекта размещена в корне репозитория (README, API.md), но frontend-специфика там описана поверхностно.

### Инфраструктура разработки
- **Скрипты `package.json`:** `dev`, `build`, `start`, `lint`, `lint:fix`, `type-check`, `format`. Этого достаточно для CI, но тестовых скриптов нет.
- **Среда:** `.env.local` задаёт `NEXT_PUBLIC_API_URL`. Next автоматически подхватывает значение и пробрасывает в браузер.
- **Хуки/CI:** pre-commit и CI-конфигурации отсутствуют, поэтому качество зависит от дисциплины разработчиков.
- **Docker:** многоступенчатый файл (base → builder → runner) собирает standalone build и запускает `node server.js`. Для production стоит добавить HEALTHCHECK и переменные окружения для baseURL.

## 🔧 Ключевые компоненты
### 1. `src/app/page.tsx` — главная страница
Назначение: ленд-страница с навигацией, CTA и популярными категориями. Интеграции: Zustand (`useAuth`) для определения статуса пользователя, Next Link для навигации.
```tsx
return (
  <div className="flex gap-4">
    {isAuthenticated ? (
      <>
        <Link href="/listings" className="text-blue-600 hover:text-blue-800">Browse</Link>
        <Link href="/my-listings" className="text-blue-600 hover:text-blue-800">My Listings</Link>
        <Link href="/profile" className="text-blue-600 hover:text-blue-800">Profile</Link>
      </>
    ) : (
      <>
        <Link href="/login" className="text-blue-600 hover:text-blue-800">Login</Link>
        <Link href="/register" className="bg-primary text-white px-4 py-2 rounded">Register</Link>
      </>
    )}
  </div>
);
```
Основные пропсы: отсутствуют (страница читает состояние напрямую). Зависимости: Zustand store и Next Link.

### 2. `src/app/listings/page.tsx` — каталог объявлений
Назначение: поиск и отображение объявлений с фильтрами по тексту, региону и цене. Интеграции: axios API, тип `Listing`.
```tsx
{listings.map((listing) => (
  <Link key={listing.id} href={`/listings/${listing.id}`} className="bg-white rounded-lg shadow">
    <div className="relative h-48 bg-gray-200">
      {listing.images.length > 0 && (
        <img src={listing.images[0].url} alt={listing.title} className="w-full h-full object-cover" />
      )}
    </div>
    <div className="p-4">
      <h3 className="font-semibold text-lg text-gray-900 mb-2 line-clamp-2">{listing.title}</h3>
      <p className="text-gray-600 text-sm mb-4 line-clamp-2">{listing.description}</p>
    </div>
  </Link>
))}
```
Основные состояния: `listings`, `loading`, фильтры (`search`, `region`, `minPrice`, `maxPrice`). Потенциальное улучшение — вынесение карточки в отдельный компонент и добавление skeleton state.

### 3. `src/store/auth.ts` — Zustand store аутентификации
Назначение: централизованное хранение пользователя и токена, управление логином/регистрацией. Интеграции: axios API, `localStorage`.
```ts
register: async (email, password, firstName, lastName) => {
  set({ isLoading: true, error: null });
  try {
    const response = await api.post('/auth/register', { email, password, firstName, lastName });
    localStorage.setItem('accessToken', response.data.accessToken);
    set({ user: response.data.user, isAuthenticated: true, isLoading: false });
  } catch (error: any) {
    set({
      error: error.response?.data?.message || 'Registration failed',
      isLoading: false,
    });
    throw error;
  }
},
```
API: методы `register`, `login`, `logout`, `checkAuth`. Улучшения: валидация форм, refresh-token, SSR-friendly хранение (cookies).

### 4. `src/lib/api.ts` — HTTP-клиент
Назначение: единая точка работы с REST API бэкенда. Интеграции: Axios, `localStorage`, ENV.
```ts
const api: AxiosInstance = axios.create({
  baseURL: API_URL,
  headers: { 'Content-Type': 'application/json' },
});

api.interceptors.request.use((config) => {
  const token = localStorage.getItem('accessToken');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});
```
Преимущества: централизованный заголовок Authorization, лёгкое расширение (response interceptors). Важно добавить защиту от SSR и обработку 401/refresh.

## 📋 Выводы и рекомендации
**Сильные стороны:**
- Чёткая, минималистичная структура: App Router + слой стора/типов/инфраструктуры.
- Строгий TypeScript и базовые линтинговые правила.
- Tailwind обеспечивает быструю верстку и адаптивность.
- Zustand store инкапсулирует всю auth-логику и уведомляет UI об ошибках/загрузке.

**Зоны улучшения:**
1. **SSR-совместимость:** перенести обращения к `localStorage` внутрь условных блоков (`if (typeof window !== 'undefined')`) или использовать cookies через Next middleware.
2. **Повторное использование UI:** вынести карточку объявления, формы и навбар в отдельные компоненты, что облегчит расширение и тестирование.
3. **Менеджмент данных:** добавить клиенский кеш (SWR/React Query) и дебаунс фильтров, чтобы минимизировать запросы.
4. **Тесты и CI:** настроить как минимум unit-тесты (Jest/RTL) для стора и страниц, а также добавить pre-commit/CI для `lint` + `type-check`.
5. **Обработка ошибок:** показывать пользователю сетевые ошибки (toasts, inline alerts), а не только `console.error`.
6. **Безопасность и UX:** реализовать `logout` на сервере, добавить подтверждение действий и сообщения успеха (можно использовать уже установленный `react-hot-toast`).
7. **Интернационализация и доступность:** добавить `aria-live` для сообщений об ошибках, лейблы для инпутов; рассмотреть i18n, так как проект ориентируется на региональные объявления.

**Оценка сложности:** текущая кодовая база подходит для уровня *junior+/middle* — технологии современны (Next14, TypeScript, Zustand), но количество модулей и архитектурных решений ограничено, что делает проект доступным для быстрого онбординга. Для перехода в senior-уровень потребуются дополнительные слои (сложный state management, SSR/ISR, кеши, тесты, CI/CD).
